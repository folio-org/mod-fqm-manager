{
  name: 'src_srs_records_lb',
  dependsOn: [['${tenant_id}_mod_source_record_storage', 'records_lb']],
  sql: "SELECT * FROM ${tenant_id}_mod_source_record_storage.records_lb where state != 'OLD'",
}
