{
  name: 'src__catalog__entities_links__authority',
  dependsOn: [
    [
      '${tenant_id}_mod_entities_links',
      'authority',
    ],
  ],
  sql: 'SELECT * FROM ${tenant_id}_mod_entities_links.authority',
}
