// TODO: define this in external repo instead
{
  name: 'src__circulation_storage__request',
  dependsOn: [
    [
      '${tenant_id}_mod_circulation_storage',
      'request',
    ],
  ],
  sql: 'SELECT * FROM ${tenant_id}_mod_circulation_storage.request',
}
